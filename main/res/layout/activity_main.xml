<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF"
    android:gravity="center_horizontal"
    android:orientation="vertical"
    android:padding="24dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:text="mBot Ranger Controller"
        android:textColor="#000000"
        android:textSize="20sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/textStatus"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="24dp"
        android:text="Status: Not Connected"
        android:textColor="#666666"
        android:textSize="16sp" />

    <Button
        android:id="@+id/btnConnect"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="24dp"
        android:backgroundTint="#2196F3"
        android:text="🔗 Connect to mBot"
        android:textColor="#FFFFFF" />

    <Button
        android:id="@+id/btnLineFollow"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="12dp"
        android:backgroundTint="#4CAF50"
        android:text="▶ Line Following"
        android:textColor="#FFFFFF" />

    <Button
        android:id="@+id/btnObstacleAvoid"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="12dp"
        android:backgroundTint="#FF9800"
        android:text="🚧 Obstacle Avoidance"
        android:textColor="#FFFFFF" />

    <Button
        android:id="@+id/btnHandFollow"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="12dp"
        android:backgroundTint="#9C27B0"
        android:text="✋ Hand Following"
        android:textColor="#FFFFFF" />

    <Button
        android:id="@+id/btnStop"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="24dp"
        android:backgroundTint="#F44336"
        android:text="⏹ Stop"
        android:textColor="#FFFFFF" />

    <!-- NEW: show the last‐reported distance -->
    <TextView
        android:id="@+id/textDistance"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:text="Distance: —"
        android:textColor="#333333"
        android:textSize="16sp" />

    <!-- NEW: ask mBot for its encoder distance -->
    <Button
        android:id="@+id/btnDistance"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:backgroundTint="#03A9F4"
        android:text="📏 Get Distance"
        android:textColor="#FFFFFF" />

</LinearLayout>
